{
  "name": "slots-game",
  "version": "1.0.0",
  "description": "A Pixi.js based slot machine game with sound, background images, and an Express server.",
  "main": "build/server/index.js",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "test": "echo \"No tests specified\" && exit 0",
    "eject": "echo \"Eject is not available for this project\" && exit 0",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "@pixi/sound": "^6.0.1",
    "express": "^4.18.2",
    "pixi.js": "^8.8.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.16.18",
    "gh-pages": "^5.0.0",
    "typescript": "^5.0.0",
    "vite": "^4.0.0"
  }
}
